# DSH Desktop — Release Notes

---

# Release Notes — DSH Desktop v1.0.13

## ✨ 新增功能

- **标题栏 DeepSeek 余额徽章**：标题栏「帮助」按钮右侧常驻余额徽章，实时展示 DeepSeek 账户余额（¥ 符号 + 金额，超过万元自动折算为「万」单位），无需打开网页或终端查询。余额数据由主进程直接解析 DSH 本地凭据调用官方接口获取，API Key 不出主进程。
- **七种状态一目了然**：正常（绿点）、加载中（灰点闪烁）、余额不足（红点脉冲 + 红色金额，最醒目）、API Key 失效（橙字）、未配置（灰点）、中转站（检测到自定义 API 地址时静默降级为只读提示）、网络异常（灰点）。
- **悬停查看明细**：鼠标悬停徽章弹出明细面板（当前余额、缓存条目数、查询来源、最近结果等），面板由主进程注入 DSH 内容页展示，随系统 / 应用主题自动切换深浅配色，移开鼠标即收起。
- **点击手动刷新**：点击徽章立即重新查询余额；后台另有定时轮询（自动更新徽章与托盘菜单）。
- **托盘菜单联动**：右键托盘图标新增余额只读项与「刷新余额」项，不打扰前台即可掌握与刷新余额。
- **窄窗口自适应**：窗口宽度不足时自动隐藏 ¥ 符号位，保住金额显示与窗口控制按钮的间距。

## 🛠 问题修复

- **修复徽章金额前出现两个 ¥ 符号的显示错误**（符号位与格式化函数自带前缀叠加所致）。
- **修复悬停徽章时 DSH 区域异常显示（黑屏）的问题**：明细面板改为注入 DSH 内容页展示，不再依赖隐藏内容子视图的方案。
- **修复窗口偶发不显示的问题**：`ready-to-show` 事件若在监听挂载前触发或丢失，启动 3 秒后兜底强制显示窗口（此前现象：应用启动后无窗口，进程与托盘均正常）。

## 📦 安装包

| 文件 | 说明 |
| --- | --- |
| `DSH-Desktop-Setup-1.0.13.exe` | Windows x64 安装程序，需以管理员权限运行 |

**安装包校验值**（打包后补充）：

| 算法 | 值 |
| --- | --- |
| SHA256 | 待补充 |
| SHA512 (base64) | 待补充 |

> 双击安装包即可从旧版本升级（保留任务栏固定图标）。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.11

## 🔧 运行时升级

- **内置 Node.js 由 v22.19.0 升级到 v24.21.0（LTS「Krypton」最新补丁版，win-x64）**：DSH 服务子进程、内置 npm/npx、在线修复与运行包更新全部随之切换到新运行时。原生模块 ABI 由 127 变为 137，内置 npm 由 10.9.3 升级到 11.19.0（node-gyp 12.4.0）。
- **在线更新 / 修复的原生模块编译自动对齐新 ABI**：更新/修复的 `npm install` 由内置 Node 执行，并把内置 Node 目录前置到 PATH，node-gyp 按运行它的 Node 版本（现为 v24.21.0 / ABI 137）编译，原生模块天然对齐新运行时。已用客户端内真实的「更新 DSH」流程实测（v0.1.5-rc.1 → v0.1.5-rc.2：下载 / 完整性校验 / 安装 518 包 / ABI 校验 / 激活 / 重启全部通过），并对 nan 源码编译型模块（fs-ext）单独做过「按 v24 编译 + 在内置 Node 下加载」探针。
- **出厂 DSH 包已在新运行时下重建**：`resources/dsh-bundled/` 由 `preinstall-dsh` 在 v24 下重新生成，避免首启时因 ABI 不一致报 `ERR_DLOPEN_FAILED`。
- **下载脚本增加完整性与版本双重校验**：`scripts/download-node.js` 现在先比对压缩包 SHA256（与官方 SHASUMS256.txt 钉值），通过后才解压，并在落地后比对 `node -v` 与脚本内 `NODE_VERSION`，任一不一致直接失败退出；旧 `resources/node/` 的清理已改到校验通过之后，校验失败不会破坏现有可用的内置 Node。
- **关于模态框可查看运行时**：「帮助 → 关于 DSH Desktop」新增一行「内置 Node 运行时：vX.Y.Z（ABI NNN）」，由内置 node.exe 动态查询，便于反馈问题时确认实际生效的运行时与 ABI。

## ⚠️ 升级须知

- 直接从旧版本双击安装包升级即可。若你此前通过「在线更新 / 修复」拿到过按旧内置 Node（ABI 127）编译的 DSH 缓存，升级后应用会在启动时自动失效该缓存并回退到内置版本，随后的更新会按新 ABI 重新编译；也可手动删除 `%LOCALAPPDATA%\DSH Desktop\dsh-cache\dsh` 立即清理。
- Node.js 24 要求 Windows 10 及以上，与本客户端既有系统要求一致，无新增限制。安装包体积因运行时更新略有增加（约 +8 MB）。

## 📦 安装包

| 文件                              | 说明                         |
| ------------------------------- | -------------------------- |
| `DSH-Desktop-Setup-1.0.11.exe` | Windows x64 安装程序，需以管理员权限运行 |

**安装包校验值**（152,256,433 字节）：

| 算法 | 值 |
| --- | --- |
| SHA256 | `034ebcdcaf62b7daaa76c1aee5b17b9f44996625f6349d85286a2fdce063a08b` |
| SHA512 (base64) | `6T9K6y0No5p/EcGjEJBBWGD/hxDeIqsXqiuGQMehdzSbgTOSxk1eQJegixwqGfSIS4oovVBdYzOyutJRJXKolA==` |

> 双击安装包即可从旧版本升级（保留任务栏固定图标）。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.10

## ✨ 体验优化

- **启动等待页焕新为骨架屏**：从居中转圈 spinner 替换为模仿 DSH 主界面轮廓的抽象骨架屏——左侧会话列表色块、居中 hero 占位（logo + 标题 + 徽标）、输入卡片轮廓、底部一行实时状态文字。色块上叠加从左到右缓慢扫过的微光渐变（1.8s 一轮），营造「即将就绪」的视觉预期。
- **主题自适应**：骨架屏色板随系统 / 应用主题（浅色 / 深色）自动切换，首屏无闪烁（`data-theme` 在 preload `document_start` 同步打标）。
- **无障碍降级**：系统「动画效果」关闭时（Windows 设置 → 辅助功能 → 视觉特效），微光动画自动降级为静态色块（`prefers-reduced-motion: reduce` 媒体查询）。
- **精简等待期信息**：移除等待页上的应用版本号与 DSH 版本号展示（这两项在「帮助 → 关于」模态框中仍可查到），仅保留一行实时状态文字（「正在启动 DSH 服务...」 / 「服务已启动，正在等待就绪...」）。

## 📦 安装包

| 文件                              | 说明                         |
| ------------------------------- | -------------------------- |
| `DSH-Desktop-Setup-1.0.10.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 双击安装包即可从旧版本升级（保留任务栏固定图标）。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.9

## 🐛 问题修复

- **修复在线更新后 DSH 启动即崩溃（`ERR_DLOPEN_FAILED`）的问题**：更新到依赖原生模块 `fs-ext` 的 DSH 版本（如 `0.1.3-alpha.x`，经 `@deepseek-ai/dsh-session-persistence-jsonl` 引入）后，`fs-ext` 的 `node-gyp` 编译在安装时命中了系统 PATH 上的 Node（版本可能与内置运行时不同），编出的 `.node` 与内置 Node.js（v22 / ABI 127）不匹配，DSH 子进程启动即退出（code=1）。现已在更新/修复的 `npm install` 环节把内置 Node 目录前置到 PATH，并显式钉住 `node-gyp` 的目标版本与架构（`npm_config_target` / `arch` / 国内镜像 `disturl`），确保原生模块严格按内置 Node 的 ABI 编译。

## ✨ 健壮性增强

- **更新安装后校验原生模块 ABI**：安装完成、写入缓存前，逐个用内置 Node 加载 `build/Release/*.node`，一旦发现 ABI 不匹配立即中止，绝不把「启动即崩」的坏缓存激活上线。
- **坏缓存启动自愈回退**：若启动时检测到缓存内原生模块 ABI 不匹配，自动将失效缓存改名 aside 并回退到内置 DSH 版本重启，不再反复崩溃循环；下次更新会重新拉取并正确编译。

## 📦 安装包

| 文件                             | 说明                         |
| ------------------------------ | -------------------------- |
| `DSH-Desktop-Setup-1.0.9.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 双击安装包即可从旧版本升级（保留任务栏固定图标）。
> 若你此前已更新到含 `fs-ext` 的 DSH 版本并遇到启动崩溃，升级到 1.0.9 后应用会自动失效坏缓存并回退到可用的内置版本；也可手动删除 `%LOCALAPPDATA%\DSH Desktop\dsh-cache\dsh` 目录立即恢复。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.8

## ✨ 新增功能

- **系统托盘图标**：应用启动后在 Windows 托盘区常驻显示应用图标（悬停显示 “DSH Desktop”），右键弹出原生菜单，仅含两项：
  - **打开主界面**：将主窗口从最小化 / 隐藏 / 失焦状态恢复、显示并聚焦；窗口已销毁时自动重建。
  - **退出**：先弹出退出确认框，确认后退出应用；取消则继续运行。
- **窗口关闭退出确认**：点击标题栏右上角关闭按钮（或 Alt+F4）不再直接退出，而是弹出与托盘「退出」相同的确认框（「退出」/「取消」，默认聚焦取消），避免误触丢失正在运行的 DSH 会话；确认后走既有清理链路（自动结束 DSH 后台进程）。

## 🛠 优化与调整

- 所有退出路径（托盘退出 / 窗口关闭 / 确认退出）均先销毁托盘图标再退出，避免 Windows 托盘区残留幽灵图标。
- 退出确认受互斥锁保护，重复触发不会叠加弹框；取消后锁自动复位，可再次触发。

## 📦 安装包

| 文件                             | 说明                         |
| ------------------------------ | -------------------------- |
| `DSH-Desktop-Setup-1.0.8.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 双击安装包即可从旧版本升级（保留任务栏固定图标）。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.7

## ✨ 体验优化与功能增强

- **设置菜单简化为原生主题菜单**：移除原自定义三级浮层（「设置」标题 → 外观 → 明暗模式），「设置」按钮改为弹出 Windows 原生菜单：顶级仅「主题」一项，子菜单为跟随系统 / 浅色 / 深色三个单选项（当前值带单选点），选中后主题立即生效；原生菜单外观跟随 Windows 系统主题。
- **DSH 更新检查升级为双源并行取最大**：由「国内镜像优先、失败才回退」改为并行查询 npmmirror 与 npmjs 的 dist-tags 并取版本最大者，消除镜像同步滞后窗口内的更新漏检（单源失败用另一源，双源失败才报错）。
- **手动检查附加上游 GitHub 版本告知**：手动检查 DSH 更新时并行查询上游 `deepseek-ai/deepseek-harness` Releases（`dsh-v*` tag）；若上游已有新版但尚未发布到 npm，对话框附加「尚未发布到 npm」告知；自动横幅与轮询仍只认 npm 可安装版本。

## 🐛 问题修复

- **修复 DSH 运行包更新因 npm 本地陈旧缓存必然失败的问题**：更新时 `npm install` 报 `ETARGET（No matching version found）` 退出码 1，且重试无法自愈。根因是更新流程传了 `--prefer-offline`，npm 对已过期的包元数据缓存不做重验证直接复用，导致解析不到上游新发布的版本。现已移除该参数，恢复 npm 默认元数据重验证策略（tarball 仍走内容寻址缓存，更新速度基本不受影响）。
- **npm 依赖解析失败提示更友好**：出现 `ETARGET/notarget` 时提示「依赖的目标版本在 registry 中不存在（上游可能尚未发布或镜像未同步），请稍后重试」。
- **修复更新后依赖树不完整导致 DSH 无法启动的问题**：旧逻辑复用上次缓存的 node_modules 做增量安装，实测 npm 对复用旧树的增量 diff 会漏装依赖（0.1.2-rc.1 的 sharp 0.35.4 缺失其依赖 `@img/colour`，启动报 ERR_MODULE_NOT_FOUND）。现已改为每次全量安装（tarball 走内容寻址缓存，实测仅 24 秒，速度基本不受影响）。

## 📦 安装包

| 文件                             | 说明                         |
| ------------------------------ | -------------------------- |
| `DSH-Desktop-Setup-1.0.7.exe` | Windows x64 安装程序，需以管理员权限运行 |

## ⚠️ 老版本升级提示

- ≤1.0.6 版本如遇 DSH 更新报 `ETARGET` 失败，可删除 `%LOCALAPPDATA%\DSH Desktop\npm-cache\_cacache` 目录（纯缓存、可再生）后重试更新即可恢复；或直接升级到本版本。

---

# Release Notes — DSH Desktop v1.0.6

## 🚀 更新内容

### ✨ 新增功能

- **明暗主题切换**：新增浅色 / 深色 / 跟随系统三种主题模式，设置持久化保存；切换时标题栏、窗口控制按钮（titleBarOverlay）与 DSH 页面同步换肤，跟随系统模式下自动响应系统主题变化。
- **自定义设置菜单**：标题栏新增「设置」按钮，弹出多级设置菜单（设置 → 外观 → 明暗模式），当前选中项带勾选标记。
- **标题栏帮助菜单**：新增「帮助」按钮，弹出原生菜单：检查更新（DSH 运行包 / 客户端 / 全部）、更新日志（子菜单）、关于。
- **更新日志**：帮助菜单内可查看 DSH 运行包与客户端两个项目的 GitHub Releases 日志，安全 Markdown 渲染（全文转义 + 标签白名单，链接仅允许 http(s)）。
- **关于模态框**：展示客户端版本号与 DSH 运行包版本号，内置带 GitHub octocat 图标的仓库跳转按钮。
- **标题栏左侧应用图标**：标题栏左侧展示应用图标（主进程读取 icon.ico 转 data URL 下发）。

### 🛠 优化与调整

- 「帮助」/「设置」按钮移至标题栏左侧（应用图标与标题文字之后），符合 Windows 桌面应用功能入口靠左的常规布局。
- 移除 DSH 页面右上角 GitHub 浮动图标（遮挡页面内容），GitHub 仓库入口收敛到「关于」模态框，简化更新横幅注入逻辑。
- 启动等待页与错误页配色接入主题体系，随明暗模式切换。
- IPC 安全加固延续：敏感通道均受发送方信任校验保护，帮助菜单仅允许标题栏页面触发。
- 文档同步：AGENTS.md 更新进程拓扑、IPC 通道表与踩坑记录。

## 📦 安装包

| 文件                             | 说明                         |
| ------------------------------ | -------------------------- |
| `DSH-Desktop-Setup-1.0.6.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 安装包基于内置 Node.js v22.19.0，内置 DSH 包，首次启动即可直接使用。双击安装包即可从旧版本升级（保留任务栏固定图标）。

## 📖 使用说明

1. 双击 `DSH-Desktop-Setup-1.0.6.exe` 完成安装。
2. 双击桌面图标启动，等待 DSH 服务就绪后自动加载 Web 界面。
3. 点击标题栏「设置」→「外观」→「明暗模式」可切换浅色 / 深色 / 跟随系统。
4. 点击标题栏「帮助」可检查更新、查看更新日志或打开「关于」。
5. 如遇 DSH 包缺失或版本提醒，可使用界面内「在线修复 / 立即更新」功能。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.1

## 🚀 更新内容

### ✨ 新增功能

- **DSH 内核在线更新**：应用启动后自动检测 DSH 内核是否有新版本，发现新版本时弹出提示，支持一键在线更新，无需手动重新部署。
- **桌面应用自更新**：启动时自动检查 DSH Desktop 应用自身是否有新版本，检测到后自动下载安装包，并在界面右上角显示"有更新"徽标，点击即可进入安装流程。
- **GitHub 仓库入口**：DSH Web 界面右上角新增 GitHub 图标，点击后在系统默认浏览器打开项目仓库。
- **版本信息展示**：启动等待页与窗口标题新增当前版本号显示（应用版本 + DSH 内核版本），便于确认运行版本。

### 🛠 优化与稳定性

- 更新检测与下载**不阻塞启动流程**，均以异步方式在后台进行，保持首屏加载速度。
- 应用自更新仅在**打包环境**生效，开发模式下自动跳过，不影响日常开发调试。
- 更新磁盘落地采用增量续传逻辑：安装包已存在时自动跳过重复下载。

## 📦 安装包

| 文件                            | 说明                         |
| ----------------------------- | -------------------------- |
| `DSH Desktop Setup 1.0.1.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 安装包基于内置 Node.js v22.19.0，内置 DSH 包，首次启动即可直接使用。

## 📖 使用说明

1. 双击 `DSH Desktop Setup 1.0.1.exe` 完成安装。
2. 双击桌面图标启动，等待 DSH 服务就绪后自动加载 Web 界面。
3. 如遇 DSH 包缺失或版本提醒，可使用界面内"在线修复 / 立即更新"功能。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.0

> 🎉 **首个正式版本！** DSH Desktop 将 DeepSeek Harness Web 封装为 Windows 桌面应用，双击即可使用，无需接触终端或浏览器。

## 🚀 新功能

### 一键启动，开箱即用

- 将 `npx @deepseek-ai/dsh web` 封装为原生 Windows 桌面应用，双击图标即可完成服务启动与界面加载。
- **内置 Node.js v22.19.0** 与**预装 DSH 包**，无需手动安装任何依赖，首次启动即可直接使用。

### 智能进程与端口管理

- DSH 服务运行于**独立子进程**，与主进程隔离，应用关闭时自动清理全部进程树，避免残留。
- 默认监听 `127.0.0.1:3080`，端口被占用时**自动递增**寻找可用端口，支持复用已运行的 DSH 服务；端口被非 DSH 进程占用时自动切换。
- 采用 IPv4 绑定与**逐端口健康检查**，确保服务就绪后才加载界面，最多重试 20 个端口。

### 良好的启动体验

- **启动等待页**：动态显示启动进度文案，含内置加载动画。
- **错误处理页**：启动失败时展示具体错误原因，支持"重试"与 **"在线修复"** 一键恢复。
- **在线修复**：DSH 包缺失或损坏时，可从 npm registry 自动下载并解压恢复。

### 工程化与可靠性

- 基于 Electron 33 + electron-vite + TypeScript（严格模式）。
- 打包产物为 NSIS x64 安装包，支持自定义安装目录、以管理员权限运行，满足 DSH 符号链接创建需求。
- 体积经过优化（剔除内置 Node 冗余文件），显著减小安装包体积。

## 📦 安装包

| 文件                            | 说明                         |
| ----------------------------- | -------------------------- |
| `DSH Desktop Setup 1.0.0.exe` | Windows x64 安装程序，需以管理员权限运行 |

## 📖 使用说明

1. 双击 `DSH Desktop Setup 1.0.0.exe` 完成安装。
2. 双击桌面图标，等待 DSH 服务就绪后自动加载 Web 界面（首次启动下载组件约需 1-3 分钟）。
3. 如遇错误，可点击"重试"或"在线修复"。

## 🔮 技术栈

Electron 33 · electron-vite · TypeScript · electron-builder · 内置 Node.js v22.19.0

## 📜 开源协议

本项目基于 **MIT** 协议开源。

## 🐛 反馈

任何问题欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。