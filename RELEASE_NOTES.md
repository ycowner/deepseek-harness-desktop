# DSH Desktop — Release Notes

---

# Release Notes — DSH Desktop v1.0.7

## ✨ 体验优化与功能增强

- **设置菜单简化为原生主题菜单**：移除原自定义三级浮层（「设置」标题 → 外观 → 明暗模式），「设置」按钮改为弹出 Windows 原生菜单：顶级仅「主题」一项，子菜单为跟随系统 / 浅色 / 深色三个单选项（当前值带单选点），选中后主题立即生效；原生菜单外观跟随 Windows 系统主题。
- **DSH 更新检查升级为双源并行取最大**：由「国内镜像优先、失败才回退」改为并行查询 npmmirror 与 npmjs 的 dist-tags 并取版本最大者，消除镜像同步滞后窗口内的更新漏检（单源失败用另一源，双源失败才报错）。
- **手动检查附加上游 GitHub 版本告知**：手动检查 DSH 更新时并行查询上游 `deepseek-ai/deepseek-harness` Releases（`dsh-v*` tag）；若上游已有新版但尚未发布到 npm，对话框附加「尚未发布到 npm」告知；自动横幅与轮询仍只认 npm 可安装版本。

## 🐛 问题修复

- **修复 DSH 运行包更新因 npm 本地陈旧缓存必然失败的问题**：更新时 `npm install` 报 `ETARGET（No matching version found）` 退出码 1，且重试无法自愈。根因是更新流程传了 `--prefer-offline`，npm 对已过期的包元数据缓存不做重验证直接复用，导致解析不到上游新发布的版本。现已移除该参数，恢复 npm 默认元数据重验证策略（tarball 仍走内容寻址缓存，更新速度基本不受影响）。
- **npm 依赖解析失败提示更友好**：出现 `ETARGET/notarget` 时提示「依赖的目标版本在 registry 中不存在（上游可能尚未发布或镜像未同步），请稍后重试」。
- **修复更新后依赖树不完整导致 DSH 无法启动的问题**：旧逻辑复用上次缓存的 node_modules 做增量安装，实测 npm 对复用旧树的增量 diff 会漏装依赖（0.1.2-rc.1 的 sharp 0.35.4 缺失其依赖 `@img/colour`，启动报 ERR_MODULE_NOT_FOUND）。现已改为每次全量安装（tarball 走内容寻址缓存，实测仅 24 秒，速度基本不受影响）。

## 📦 安装包

| 文件                             | 说明                         |
| ------------------------------ | -------------------------- |
| `DSH-Desktop-Setup-1.0.7.exe` | Windows x64 安装程序，需以管理员权限运行 |

## ⚠️ 老版本升级提示

- ≤1.0.6 版本如遇 DSH 更新报 `ETARGET` 失败，可删除 `%LOCALAPPDATA%\DSH Desktop\npm-cache\_cacache` 目录（纯缓存、可再生）后重试更新即可恢复；或直接升级到本版本。

---

# Release Notes — DSH Desktop v1.0.6

## 🚀 更新内容

### ✨ 新增功能

- **明暗主题切换**：新增浅色 / 深色 / 跟随系统三种主题模式，设置持久化保存；切换时标题栏、窗口控制按钮（titleBarOverlay）与 DSH 页面同步换肤，跟随系统模式下自动响应系统主题变化。
- **自定义设置菜单**：标题栏新增「设置」按钮，弹出多级设置菜单（设置 → 外观 → 明暗模式），当前选中项带勾选标记。
- **标题栏帮助菜单**：新增「帮助」按钮，弹出原生菜单：检查更新（DSH 运行包 / 客户端 / 全部）、更新日志（子菜单）、关于。
- **更新日志**：帮助菜单内可查看 DSH 运行包与客户端两个项目的 GitHub Releases 日志，安全 Markdown 渲染（全文转义 + 标签白名单，链接仅允许 http(s)）。
- **关于模态框**：展示客户端版本号与 DSH 运行包版本号，内置带 GitHub octocat 图标的仓库跳转按钮。
- **标题栏左侧应用图标**：标题栏左侧展示应用图标（主进程读取 icon.ico 转 data URL 下发）。

### 🛠 优化与调整

- 「帮助」/「设置」按钮移至标题栏左侧（应用图标与标题文字之后），符合 Windows 桌面应用功能入口靠左的常规布局。
- 移除 DSH 页面右上角 GitHub 浮动图标（遮挡页面内容），GitHub 仓库入口收敛到「关于」模态框，简化更新横幅注入逻辑。
- 启动等待页与错误页配色接入主题体系，随明暗模式切换。
- IPC 安全加固延续：敏感通道均受发送方信任校验保护，帮助菜单仅允许标题栏页面触发。
- 文档同步：AGENTS.md 更新进程拓扑、IPC 通道表与踩坑记录。

## 📦 安装包

| 文件                             | 说明                         |
| ------------------------------ | -------------------------- |
| `DSH-Desktop-Setup-1.0.6.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 安装包基于内置 Node.js v22.19.0，内置 DSH 包，首次启动即可直接使用。双击安装包即可从旧版本升级（保留任务栏固定图标）。

## 📖 使用说明

1. 双击 `DSH-Desktop-Setup-1.0.6.exe` 完成安装。
2. 双击桌面图标启动，等待 DSH 服务就绪后自动加载 Web 界面。
3. 点击标题栏「设置」→「外观」→「明暗模式」可切换浅色 / 深色 / 跟随系统。
4. 点击标题栏「帮助」可检查更新、查看更新日志或打开「关于」。
5. 如遇 DSH 包缺失或版本提醒，可使用界面内「在线修复 / 立即更新」功能。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.1

## 🚀 更新内容

### ✨ 新增功能

- **DSH 内核在线更新**：应用启动后自动检测 DSH 内核是否有新版本，发现新版本时弹出提示，支持一键在线更新，无需手动重新部署。
- **桌面应用自更新**：启动时自动检查 DSH Desktop 应用自身是否有新版本，检测到后自动下载安装包，并在界面右上角显示"有更新"徽标，点击即可进入安装流程。
- **GitHub 仓库入口**：DSH Web 界面右上角新增 GitHub 图标，点击后在系统默认浏览器打开项目仓库。
- **版本信息展示**：启动等待页与窗口标题新增当前版本号显示（应用版本 + DSH 内核版本），便于确认运行版本。

### 🛠 优化与稳定性

- 更新检测与下载**不阻塞启动流程**，均以异步方式在后台进行，保持首屏加载速度。
- 应用自更新仅在**打包环境**生效，开发模式下自动跳过，不影响日常开发调试。
- 更新磁盘落地采用增量续传逻辑：安装包已存在时自动跳过重复下载。

## 📦 安装包

| 文件                            | 说明                         |
| ----------------------------- | -------------------------- |
| `DSH Desktop Setup 1.0.1.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 安装包基于内置 Node.js v22.19.0，内置 DSH 包，首次启动即可直接使用。

## 📖 使用说明

1. 双击 `DSH Desktop Setup 1.0.1.exe` 完成安装。
2. 双击桌面图标启动，等待 DSH 服务就绪后自动加载 Web 界面。
3. 如遇 DSH 包缺失或版本提醒，可使用界面内"在线修复 / 立即更新"功能。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.0

> 🎉 **首个正式版本！** DSH Desktop 将 DeepSeek Harness Web 封装为 Windows 桌面应用，双击即可使用，无需接触终端或浏览器。

## 🚀 新功能

### 一键启动，开箱即用

- 将 `npx @deepseek-ai/dsh web` 封装为原生 Windows 桌面应用，双击图标即可完成服务启动与界面加载。
- **内置 Node.js v22.19.0** 与**预装 DSH 包**，无需手动安装任何依赖，首次启动即可直接使用。

### 智能进程与端口管理

- DSH 服务运行于**独立子进程**，与主进程隔离，应用关闭时自动清理全部进程树，避免残留。
- 默认监听 `127.0.0.1:3080`，端口被占用时**自动递增**寻找可用端口，支持复用已运行的 DSH 服务；端口被非 DSH 进程占用时自动切换。
- 采用 IPv4 绑定与**逐端口健康检查**，确保服务就绪后才加载界面，最多重试 20 个端口。

### 良好的启动体验

- **启动等待页**：动态显示启动进度文案，含内置加载动画。
- **错误处理页**：启动失败时展示具体错误原因，支持"重试"与 **"在线修复"** 一键恢复。
- **在线修复**：DSH 包缺失或损坏时，可从 npm registry 自动下载并解压恢复。

### 工程化与可靠性

- 基于 Electron 33 + electron-vite + TypeScript（严格模式）。
- 打包产物为 NSIS x64 安装包，支持自定义安装目录、以管理员权限运行，满足 DSH 符号链接创建需求。
- 体积经过优化（剔除内置 Node 冗余文件），显著减小安装包体积。

## 📦 安装包

| 文件                            | 说明                         |
| ----------------------------- | -------------------------- |
| `DSH Desktop Setup 1.0.0.exe` | Windows x64 安装程序，需以管理员权限运行 |

## 📖 使用说明

1. 双击 `DSH Desktop Setup 1.0.0.exe` 完成安装。
2. 双击桌面图标，等待 DSH 服务就绪后自动加载 Web 界面（首次启动下载组件约需 1-3 分钟）。
3. 如遇错误，可点击"重试"或"在线修复"。

## 🔮 技术栈

Electron 33 · electron-vite · TypeScript · electron-builder · 内置 Node.js v22.19.0

## 📜 开源协议

本项目基于 **MIT** 协议开源。

## 🐛 反馈

任何问题欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。