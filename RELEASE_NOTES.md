# DSH Desktop — Release Notes

---

# Release Notes — DSH Desktop v1.0.1

## 🚀 更新内容

### ✨ 新增功能

- **DSH 内核在线更新**：应用启动后自动检测 DSH 内核是否有新版本，发现新版本时弹出提示，支持一键在线更新，无需手动重新部署。
- **桌面应用自更新**：启动时自动检查 DSH Desktop 应用自身是否有新版本，检测到后自动下载安装包，并在界面右上角显示"有更新"徽标，点击即可进入安装流程。
- **GitHub 仓库入口**：DSH Web 界面右上角新增 GitHub 图标，点击后在系统默认浏览器打开项目仓库。
- **版本信息展示**：启动等待页与窗口标题新增当前版本号显示（应用版本 + DSH 内核版本），便于确认运行版本。

### 🛠 优化与稳定性

- 更新检测与下载**不阻塞启动流程**，均以异步方式在后台进行，保持首屏加载速度。
- 应用自更新仅在**打包环境**生效，开发模式下自动跳过，不影响日常开发调试。
- 更新磁盘落地采用增量续传逻辑：安装包已存在时自动跳过重复下载。

## 📦 安装包

| 文件                            | 说明                         |
| ----------------------------- | -------------------------- |
| `DSH Desktop Setup 1.0.1.exe` | Windows x64 安装程序，需以管理员权限运行 |

> 安装包基于内置 Node.js v22.19.0，内置 DSH 包，首次启动即可直接使用。

## 📖 使用说明

1. 双击 `DSH Desktop Setup 1.0.1.exe` 完成安装。
2. 双击桌面图标启动，等待 DSH 服务就绪后自动加载 Web 界面。
3. 如遇 DSH 包缺失或版本提醒，可使用界面内"在线修复 / 立即更新"功能。

## 🐛 反馈

如在使用中遇到问题，欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。

---

# Release Notes — DSH Desktop v1.0.0

> 🎉 **首个正式版本！** DSH Desktop 将 DeepSeek Harness Web 封装为 Windows 桌面应用，双击即可使用，无需接触终端或浏览器。

## 🚀 新功能

### 一键启动，开箱即用

- 将 `npx @deepseek-ai/dsh web` 封装为原生 Windows 桌面应用，双击图标即可完成服务启动与界面加载。
- **内置 Node.js v22.19.0** 与**预装 DSH 包**，无需手动安装任何依赖，首次启动即可直接使用。

### 智能进程与端口管理

- DSH 服务运行于**独立子进程**，与主进程隔离，应用关闭时自动清理全部进程树，避免残留。
- 默认监听 `127.0.0.1:3080`，端口被占用时**自动递增**寻找可用端口，支持复用已运行的 DSH 服务；端口被非 DSH 进程占用时自动切换。
- 采用 IPv4 绑定与**逐端口健康检查**，确保服务就绪后才加载界面，最多重试 20 个端口。

### 良好的启动体验

- **启动等待页**：动态显示启动进度文案，含内置加载动画。
- **错误处理页**：启动失败时展示具体错误原因，支持"重试"与 **"在线修复"** 一键恢复。
- **在线修复**：DSH 包缺失或损坏时，可从 npm registry 自动下载并解压恢复。

### 工程化与可靠性

- 基于 Electron 33 + electron-vite + TypeScript（严格模式）。
- 打包产物为 NSIS x64 安装包，支持自定义安装目录、以管理员权限运行，满足 DSH 符号链接创建需求。
- 体积经过优化（剔除内置 Node 冗余文件），显著减小安装包体积。

## 📦 安装包

| 文件                            | 说明                         |
| ----------------------------- | -------------------------- |
| `DSH Desktop Setup 1.0.0.exe` | Windows x64 安装程序，需以管理员权限运行 |

## 📖 使用说明

1. 双击 `DSH Desktop Setup 1.0.0.exe` 完成安装。
2. 双击桌面图标，等待 DSH 服务就绪后自动加载 Web 界面（首次启动下载组件约需 1-3 分钟）。
3. 如遇错误，可点击"重试"或"在线修复"。

## 🔮 技术栈

Electron 33 · electron-vite · TypeScript · electron-builder · 内置 Node.js v22.19.0

## 📜 开源协议

本项目基于 **MIT** 协议开源。

## 🐛 反馈

任何问题欢迎通过 [Issues](https://github.com/ycowner/deepseek-harness-desktop/issues) 反馈。